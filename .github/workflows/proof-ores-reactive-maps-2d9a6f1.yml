name: proof ores-reactive-maps 2d9a6f1

on:
  pull_request:
    paths:
      - '.github/workflows/proof-ores-reactive-maps-2d9a6f1.yml'

permissions:
  contents: read

env:
  SUBJECT_REPOSITORY: ORESoftware/ores-reactive-maps
  SUBJECT_SHA: 2d9a6f1d418c2876d1871b22a5ae60b1376ca43e

jobs:
  typescript:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ${{ env.SUBJECT_REPOSITORY }}
          ref: ${{ env.SUBJECT_SHA }}
          path: subject
          persist-credentials: false
      - run: test "$(git -C subject rev-parse HEAD)" = "$SUBJECT_SHA"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --ignore-scripts
        working-directory: subject/src/typescript
      - run: npm test
        working-directory: subject/src/typescript
      - run: npm run build
        working-directory: subject/src/typescript

  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ${{ env.SUBJECT_REPOSITORY }}
          ref: ${{ env.SUBJECT_SHA }}
          path: subject
          persist-credentials: false
      - run: test "$(git -C subject rev-parse HEAD)" = "$SUBJECT_SHA"
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - run: go test -race ./...
        working-directory: subject/src/go

  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ${{ env.SUBJECT_REPOSITORY }}
          ref: ${{ env.SUBJECT_SHA }}
          path: subject
          persist-credentials: false
      - run: test "$(git -C subject rev-parse HEAD)" = "$SUBJECT_SHA"
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --all-targets
        working-directory: subject/src/rust

  gleam:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ${{ env.SUBJECT_REPOSITORY }}
          ref: ${{ env.SUBJECT_SHA }}
          path: subject
          persist-credentials: false
      - run: test "$(git -C subject rev-parse HEAD)" = "$SUBJECT_SHA"
      - uses: erlef/setup-beam@v1
        with:
          otp-version: '27.3'
          gleam-version: '1.15.4'
          rebar3-version: '3'
      - run: gleam test
        working-directory: subject/src/gleam
